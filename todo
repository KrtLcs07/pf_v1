Todo v1

BASE:
_Création de base de donnée (Lucas) ;;ok
    _creation des tables
    _insertion de donnée de test

Fonction:
    _insertion d'user(Dirk) ;; ok 
    _identiier user (Dirk);;  ok
    _get user (Lucas);; ok
    _get All objets (Lucas) ;;ok
    _get All catgorie (Dirk);; ok 
    _get All obejt categ (Dirk);; ok 


Page:
    _Login.php (Lucas) ;;ok
        _nom et mdp
        
    _Signin.php (Lucas) ;;ok
        _ tous les information de membre autre que pdp

    _action :
        inscription :
            fonction (Dirk) ;; (ok)
            application/traitement (Dirk) ;;(ok)


        connection :
            fonction (Dirk);; (ok)
            application/traitement (Dirk) ;;(ok)
                session start (Dirk);; (ok)

    _Creation de page model.php (Lucas) ;;ok
        _verif si page null ou non 
        _ajout de header/nav, barre de navigation 
        _ajout de footer avec ETU
        _afficher titre
        _afficher user
        _bouton de deconnection

    _Liste.php
        _Creation de vue qui relie emprunt et objets et proprietaire(Lucas);;(ok)
        _Utilisation de la fonction getallobjet(Lucas);;(ok)
        _afficher les information sur l'objet/ afficher si emprunter (Lucas) ;;ok

    _categorie_list.php
        _ajout de formulaire qui retourne sur soit meme et envoie la _categorie selectectionner (Dirk) ;;ok
        _Utilisation du meme affichage comme dans liste (Dirk) ;;ok

CSS :
    _bootstrap et css perso (Lucas,Dirk) ;;ok
    _icon bootstrap utilisé (Lucas) ;;ok


Todo V2

Fonction:
    _getObjet qui retoure les informations sur un objet (Lucas) ;;ok
    _getAllImageObjet (Lucas) ;;ok
    _getHistoriqueEmprunt (Lucas) ;;ok

Page:

    fiche_Objet.php
        _afficher information sur l'objet par la fonction get_objet (Lucas) ;;ok
        _afficher tous les images d'un objet dans un carroussel bootstrap (Lucas) ;;ok
        _ajout des historiques des emprunts avec la fonction getHistoriqueEMprunt(Lucas) ;;ok
    
    ficheMembre.php
        _afficher les informations du membre avec getUser (Lucas) ;;ok
        _afficher les objets du membre par la fonction (Lucas) ;;ok

    upload.php
        _ajout de formulaire d'ajout d'objet (Dirk) ;;ok
        _fonction d'upload et d'insertion (Dirk) ;;ok
        _essaie avec 3 image max (Dirk) ;;ok

    recherche.php
        _formulaire de recherche (Dirk);; ok
        _redirection vers soit meme et appel des fonction (Dirk) ;;ok


